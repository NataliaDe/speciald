
        <div class="form-group col-md-2">
            <label for="id_owner_category">Категория</label>
            <select  class="form-control select2-single " name="id_owner_category" id="id_owner_category" >
                <option></option>
                {% for vid in owner_categories %}
                    <option value="{{ vid.id }}" {%if is_edit_dones is defined and is_edit_dones == 1 and dones is defined and dones.id_owner_category == vid.id %}selected{%endif%}  >{{ vid.name }}</option>
                {% endfor %}
            </select>
        </div>

<div class="form-group col-md-2">
            <label for="owner_fio">Ф.И.О.</label>
            <input type="text" class="form-control"  placeholder="" name="owner_fio"  value="">

        </div>

        <div class="form-group col-md-1">

            <label for="owner_year_birthday">Год рождения</label>
            <input type="number" min="1920" max="{{now|date("Y")}}" class="form-control"  placeholder="" name="owner_year_birthday"  value="">

        </div>


        <div class="form-group col-md-2">
            <label for="owner_address" >Место жительства</label>
            <textarea class="form-control" cols="2" rows="2"  name="owner_address"></textarea>
        </div>

        <div class="form-group col-md-2">
            <label for="owner_position">Должность</label>
            <input type="text" class="form-control"  placeholder="" name="owner_position"  value="">

        </div>

        <div class="form-group col-md-2">
            <label for="owner_job" >Место работы</label>
            <textarea class="form-control" cols="2" rows="2"  name="owner_job"></textarea>
        </div>