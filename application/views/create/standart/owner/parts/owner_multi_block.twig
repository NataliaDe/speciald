

    <div class="form-group col-md-2" >

        <div class="checkbox checkbox-info" style='display:inline'>
            <input class="is_owner_multi"  type="checkbox" name="{{name_field}}" value="1" id="{{name_field}}" {%if is_edit_dones is defined and is_edit_dones == 1 and dones is defined and dones.is_owner_multi == 1  %}checked{%endif%}>
            <label for="{{name_field}}">
                несколько собственников
            </label>
        </div>
    </div>


    <div class="form-group col-md-5 div_{{name_descr}} {%if is_edit_dones is defined and is_edit_dones == 1 and dones is defined and dones.is_owner_multi == 1  %}show{%else%}hide{%endif%}">
        <label for="owner-multi" >Дополнительная информация</label>
        <textarea class="form-control" cols="2" rows="2"  name="{{name_descr}}">{%if is_edit_dones is defined and is_edit_dones == 1 and dones is defined and dones.is_owner_multi == 1 and dones.owner_multi_descr is defined and dones.owner_multi_descr is not empty  %}{{dones.owner_multi_descr}}{%endif%}</textarea>
    </div>
