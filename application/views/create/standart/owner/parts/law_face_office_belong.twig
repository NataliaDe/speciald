
    <label for="law_face_office_belong" >Ведомственная принадлежность</label>
    <select class="form-control select2-single" name="law_face_office_belong" id="law-face-office-belong-id"  >
        <option></option>
        {% for of in office_belong %}
            <option {%if (rig.office_belong_id is defined and rig.office_belong_id == of.id) or (is_edit_dones is defined and is_edit_dones == 1 and dones is defined and dones.law_face_office_belong is defined and dones.law_face_office_belong is not empty and dones.law_face_office_belong == of.id) %}selected{%endif%} value="{{ of.id }}" >{{ of.name|trim }}</option>
        {% endfor %}
    </select>
